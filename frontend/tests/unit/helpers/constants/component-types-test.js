import {constantsComponentTypes} from "frontend/helpers/constants/component-types";
import {module, test} from "qunit";

module('Unit | Helper | constants/component types');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = constantsComponentTypes([42]);
  assert.ok(result);
});

